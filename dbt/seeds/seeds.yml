version: 2

seeds:
  - name: event_types
    description: https://docs.github.com/en/webhooks-and-events/events/github-event-types
    columns:
      - name: type
        type: string
      - name: category
        type: string
      - name: desctiption
        type: string
